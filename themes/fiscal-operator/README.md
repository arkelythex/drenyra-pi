# Fiscal operator themes

The light and dark palettes are separate, complete Pi themes. They are loaded
through their explicit JSON paths in `package.json`; this directory intentionally
has no JSON variant manifest because Pi parses every discovered JSON file as a
theme.

# Tasks — pi-engram-integration

**Change:** `pi-engram-integration` (first local SDD change after the six-change program closed)
**Phase:** tasks — read-only against the repository. **The only write performed by this phase is this file.**
**Store:** `openspec` (file-backed, authoritative; `openspec/config.yaml` declares `store_mode: hybrid`)
**Repository root:** `/home/dreamcoder08/Documents/PROYECTOS/drenyra-pi`
**Authority:** `design.md` (authority for this phase, including the §6 scope correction), `specs/engram-integration/spec.md` (`REQ-ENG-001`..`004`, 8 scenarios), `proposal.md` (as corrected by design §6).

**Scope in one sentence.** Vendor and fail-closed-verify the `drenyra-engram` binary (4 platforms), give it a supervised MCP child-process lifecycle, and add one read-only institutional-context addendum to `/drenyra:context` — the active-scope pointer itself stays local, unchanged.

---

## Hard boundaries (apply phase)

**Global allowed edit surface:** `contracts/README.md`, new `contracts/engram-dependency.md`, `contracts/SHA256SUMS.json`, new `vendored/drenyra-engram/**` (binary artifacts + checksums), new `runtime/engram-pin.ts`, new `runtime/engram-client.ts`, new `__tests__/engram-pin.test.ts`, new `__tests__/engram-client.test.ts`, `extensions/register.ts` (only the `:context` handler), `capability-manifest.yaml` (only the `engram-integration` capability entry), `__tests__/capability-manifest.test.ts` (only if honestly updating the two existing guard assertions — never loosening them without new evidence), `docs/architecture/ecosystem-boundaries.md` (only the Engram-boundary lines already quoted in exploration.md), `ROADMAP.md` (only the Slice 5 checkbox), `package.json` (only a new `vendored/drenyra-engram` reference if the build needs one — no dependency entry, since it is a spawned binary, not an imported library), and this change's own artifacts.

**Prohibited:** `contracts/package-contract.md`, `contracts/runtime-dependency.md` (drenyra-ai's own contract — untouched), `openspec/specs/**` outside this change's own new `engram-integration` domain publication (that happens at archive, not apply), `MASTER_CAPABILITIES` and the capability-state counts (this change updates the existing `engram-integration` row's `state`/`evidence` fields only — never the ten-name set or counts), `PARTICIPATION_PATHS_V1` and `scripts/compute-candidate-identity.mjs`, `scripts/verify-capability-manifest.mjs`, `.git/**`, and any file inside the sibling `drenyra-engram` repository. No `accounting_*` tool call anywhere in this change's code. No `engram_save`/`accounting_record`/any write-shaped Engram tool call — this change only ever calls `engram_context` (read).

**Known, disclosed cost (not a defect, not to be silently avoided):** vendoring four compiled binaries adds roughly 4.3–4.6 MB per platform (~18 MB total, measured from the sibling repo's `dist/*.tar.gz`) to this repository's history, permanently, and grows with every future `drenyra-engram` version bump. This is materially larger than `drenyra-ai`'s vendored tarball (~640 KB, pure JS). No task in this slice avoids or hides this; `T-A-002`'s evidence must state the exact measured size added.

---

## Slice A — Contract, vendored binary, fail-closed pin verification (`REQ-ENG-001`)

- [x] T-A-001 — Read `contracts/runtime-dependency.md` and `contracts/README.md` in full to match their exact section shape (`Version`/`Status`/`Rules`/`Verification`) before authoring the new contract, per `design.md` §3. Record `git status --short` first; confirm no pre-existing modification overlaps this slice's allowlist. <!-- sdd-owner: implementation -->
- [x] T-A-002 — Copy the four platform tarballs from the sibling repo's `dist/` (`linux_amd64`, `linux_arm64`, `darwin_amd64`, `darwin_arm64`; the sibling repo itself is read-only context, never edited) into `vendored/drenyra-engram/`, named consistently with the existing `vendored/drenyra-ai-<version>.tgz` convention (e.g. `vendored/drenyra-engram-0.2.1-SNAPSHOT-6a371a9-<platform>.tar.gz`). Record the exact measured total added size (`du -sh vendored/drenyra-engram/`) as this task's evidence — this is the disclosed cost above, measured, not estimated. **Evidence: `18M vendored/drenyra-engram/` (measured).** <!-- sdd-owner: implementation -->
- [x] T-A-003 — Compute sha256 for each of the four vendored tarballs. **Revised during execution:** `contracts/SHA256SUMS.json`'s `collectCoveredFiles` walk covers only `contracts/` and `assets/schemas/`, and its vendored-artifact reconciliation is hardcoded to the single `drenyra-ai` pin (`scripts/lib/package-verify.mjs`) — there is no generic mechanism to verify entries added for `vendored/drenyra-engram/**`, and extending that script is outside this slice's allowed edit surface. Adding unverified checksum entries there would be a dishonest "looks verified but isn't" record. **Decision:** the four checksums live solely in `runtime/engram-pin.ts` (this slice's own verifier, T-A-006) and in `contracts/engram-dependency.md`'s pin table; the gap is disclosed in that contract's own "Known, disclosed gap" section, not silently left. `contracts/SHA256SUMS.json` gets exactly one new entry, for `contracts/engram-dependency.md` itself (auto-covered by the existing walk, via `--update`). <!-- sdd-owner: implementation -->
- [x] T-A-004 — Write `contracts/engram-dependency.md`: `Version: 0.2.1-SNAPSHOT-6a371a9 (pre-release, disclosed)`, `Status: tracked, not frozen` (per `preproposal.md` D2's "accept pre-alpha" spirit — matching `runtime-dependency.md`'s shape but honestly not claiming `frozen` for a `-SNAPSHOT` build), the four-platform pin table with checksums, the "never trust an ambient `drenyra-engram` on `PATH`" rule (`REQ-ENG-001` scenario 2), and the explicit Windows-not-vendored disclosure from `design.md` §1. Add a new row to `contracts/README.md`'s contract index table pointing at it. <!-- sdd-owner: implementation -->
- [x] T-A-005 — **RED.** Create `__tests__/engram-pin.test.ts` (7 cases: platform/arch resolution incl. win32 undefined; unsupported-platform verdict; missing-artifact verdict; checksum-mismatch verdict on wrong bytes; verified verdict on the real vendored artifact). Run `bun test __tests__/engram-pin.test.ts`; **RED confirmed verbatim:** `error: Cannot find module '../runtime/engram-pin.js' ... 0 pass / 1 fail / 1 error`. <!-- sdd-owner: implementation -->
- [x] T-A-006 — **GREEN.** Write `runtime/engram-pin.ts`: `resolveEngramPlatformKey`, `ENGRAM_PIN_CHECKSUMS`, `verifyEngramPin` — platform/arch → pin key, checksum verification via the existing `runtime/checksum.ts#sha256File`, fail-closed on missing/mismatched/unsupported, mirroring `runtime/doctor.ts`'s verdict-report shape. **GREEN confirmed verbatim: `7 pass / 0 fail / 15 expect() calls`.** <!-- sdd-owner: implementation -->
- [x] T-A-007 — **TRIANGULATE.** Case as originally scoped ("checksum matches but content corrupted") is a logical impossibility for sha256 (matching bytes are byte-identical to the pinned, valid artifact) — replaced with a genuinely distinct case: the artifact path exists but is a directory, not a file, exercising `sha256File`'s error path inside `verifyEngramPin` rather than a checksum-string comparison. **Confirmed verbatim: `8 pass / 0 fail / 17 expect() calls`** — passed against the existing GREEN implementation with no code change, a legitimate TDD outcome (the fail-closed try/catch already covered this branch). <!-- sdd-owner: implementation -->
- [x] T-A-008 — **REFACTOR.** Not warranted: `resolveEngramPlatformKey` is already exported and directly reusable by `runtime/engram-client.ts` (Slice B) with no duplication to extract. No change made; `8 pass / 0 fail` unchanged. <!-- sdd-owner: implementation -->
- [x] T-A-009 — Full verification set. `contracts/README.md`, `contracts/SHA256SUMS.json`, and the still-uncommitted `ROADMAP.md` stale-checkbox fix are all `PARTICIPATION_PATHS_V1` members, so the recovery pair was forced — ran to the fixed point (mirror-only pass A did not converge, exactly the documented mirror-only case; pass B converged, `--check` → current). **Post-convergence evidence: `bun test` → 781 pass / 0 fail / 3747 expect() calls; `bun run typecheck` → clean; `bun run verify:style` → OK (111 owned files); `bun run verify:capability` → OK; `--check` → current.** <!-- sdd-owner: implementation -->

## Slice B — MCP child-process lifecycle (`REQ-ENG-002`)

- [ ] T-B-001 — **RED.** Create `__tests__/engram-client.test.ts`. Case 1 (absent/corrupt binary): given `runtime/engram-pin.ts` reports verification failure, the client must report a visible, specific diagnostic and never attempt to spawn. Case 2 (healthy start): given the real vendored binary for the current platform, spawning `drenyra-engram mcp --db <tmp-sqlite-path>` and performing the MCP `initialize` handshake must succeed within a bounded timeout (record the exact timeout value and justify it against this repository's existing timeout conventions if any exist, or state the chosen value's rationale if none do). Case 3 (graceful shutdown): the child process must not remain running after the client's shutdown call — verify by process-liveness check, not by assumption. Run; record verbatim RED (module does not exist yet). <!-- sdd-owner: implementation -->
- [ ] T-B-002 — **GREEN.** Write `runtime/engram-client.ts`: spawn via `node:child_process`, one bounded-timeout `initialize` handshake over stdio (newline-delimited JSON-RPC, matching the exact wire format already proven live during design — see `design.md` §6's probe technique), graceful shutdown (signal + bounded wait, then force-kill if unresponsive), and a typed result distinguishing "verification failed" / "spawn failed" / "handshake timed out" / "healthy" — no case silently collapses into another. Run the three cases; record GREEN. <!-- sdd-owner: implementation -->
- [ ] T-B-003 — **TRIANGULATE.** Case: the child process starts, the handshake succeeds, then the process is killed externally mid-session (simulate: send SIGKILL to the child from the test) — the next call through the client must report "unhealthy/disconnected," not hang or throw an unhandled rejection. Run; record. <!-- sdd-owner: implementation -->
- [ ] T-B-004 — **REFACTOR** if warranted. Re-run all of `__tests__/engram-client.test.ts`; record unchanged pass count. Confirm no orphaned `drenyra-engram` process remains after the full test file runs (`ps` check in the test's own teardown or as a manually recorded verification step). <!-- sdd-owner: implementation -->
- [ ] T-B-005 — Full verification set for this slice (same six commands as `T-A-009`). <!-- sdd-owner: implementation -->

## Slice C — `/drenyra:context` institutional-context read, honest capability state (`REQ-ENG-003`, `REQ-ENG-004`)

- [ ] T-C-001 — Read `extensions/register.ts`'s current `:context` handler (line ~1368, per exploration) and its existing output shape (`drenyra:context: company RUC ${company} | fiscal period ${period}`, line ~633) in full before editing, so the addendum is additive and does not restructure existing, already-tested output. <!-- sdd-owner: implementation -->
- [ ] T-C-002 — **RED.** Extend the relevant existing test in `__tests__/extension.test.ts` (or add a focused new one) for `:context`: given a known RUC/period and a healthy `engram-client`, the command's output must include the institutional context `engram_context` returns, in addition to the existing RUC/period line — unchanged for callers that only checked the old line. Given `engram-client` reports unhealthy/unreachable, the command must still return the existing RUC/period line, plus a visible (not silent) note that institutional context is unavailable, and must not fail the command. Run; record RED (addendum not implemented yet). <!-- sdd-owner: implementation -->
- [ ] T-C-003 — **GREEN.** Wire `:context`'s handler to call `runtime/engram-client.ts` (spawn-or-reuse, per Slice B) and `engram_context` (read-only, `scope.kind: "company"`, the RUC/period already known from the existing local pointer) — never `accounting_current_context` (this slice makes no fiscal-effect claim per `design.md` §6's resolution) and never any write-shaped tool. Run; record GREEN. <!-- sdd-owner: implementation -->
- [ ] T-C-004 — **TRIANGULATE.** Case: `engram_context` returns successfully but with an empty/no-observations result (a real, valid RUC with no institutional history yet) — the command must report that plainly (e.g. "no institutional context recorded yet"), not an error, not a blank line that looks like a bug. Run; record. <!-- sdd-owner: implementation -->
- [ ] T-C-005 — **REFACTOR** if warranted. Re-run the full `__tests__/extension.test.ts`; record unchanged-or-improved pass count, confirm no other command's behavior shifted (`git diff` scoped to the `:context` handler only). <!-- sdd-owner: implementation -->
- [ ] T-C-006 — Update `capability-manifest.yaml#/capabilities/engram-integration`: `state` (from `partial` to whatever this slice's delivered scope honestly supports — likely still `partial`, since proposal-informing reads and scope persistence both remain undelivered; do not claim more than `T-C-002`–`T-C-004`'s evidence supports), `evidence.sources` (add `runtime/engram-pin.ts`, `runtime/engram-client.ts`, the `:context` handler change), `evidence.limitation` (rewritten to state precisely what changed: institutional-context read added for an already-known scope; the active-scope pointer is still local; proposal-informing reads are still not evidenced). <!-- sdd-owner: implementation -->
- [ ] T-C-007 — Run `__tests__/capability-manifest.test.ts` in full, specifically the two existing guards (lines ~961-978 `operational=end-to-end` block, ~1095-1118 ownership-escalation block per `exploration.md`'s citation). If either now legitimately needs updating because `T-C-006`'s honest state genuinely crosses a line those guards check, update the guard's own assertion with a clear diff explaining why — never loosen a guard to make a test pass without that explanation recorded here. Record the verbatim before/after. <!-- sdd-owner: implementation -->
- [ ] T-C-008 — Update `docs/architecture/ecosystem-boundaries.md`'s Engram-boundary lines (the ones quoted verbatim in `exploration.md` §"What exists to integrate with") to state the delivered scope precisely — not more, not less — and check `ROADMAP.md`'s Slice 5 line: check it off **only if** this slice's scope is what "Slice 5" was understood to mean, or reword it to reflect that Slice 5's remaining depth (proposal-informing reads, scope persistence) is still open — do not silently check a box wider than what shipped. <!-- sdd-owner: implementation -->
- [ ] T-C-009 — Full verification set for this slice (same six commands as `T-A-009`), plus explicit confirmation that no `accounting_*` tool name and no write-shaped `engram_*` tool name (`engram_save`, `engram_reject`, `engram_void`, `engram_supersede`) appears anywhere in the diff (`git diff | grep -n "accounting_\|engram_save\|engram_reject\|engram_void\|engram_supersede"` → expect no matches outside this file's own citation of them). <!-- sdd-owner: implementation -->

---

## Review Workload Forecast

```
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain (established convention in this repository, SDD 6)
400-line budget risk: Medium
```

Estimated changed lines (source, not binary): Slice A ≈ 300–400 (new contract doc, pin module, tests); Slice B ≈ 300–400 (client module, tests); Slice C ≈ 150–250 (handler addendum, manifest, docs, guard-test updates). Each slice is individually inside the 400-line budget, but the natural unit boundaries (contract+binary / lifecycle / integration) match this repository's own precedent (SDD 6's three slices) for keeping each PR independently reviewable — chaining is recommended for clean review boundaries, not because any single slice is forecast to exceed budget. The vendored binaries themselves are not source lines; a reviewer should expect four new binary blobs (~18 MB total) in Slice A's diff, disclosed in `T-A-002`.

---

## Not to be re-planned / already delivered

Nothing — this is the first apply pass of a new change. `runtime/context.ts`'s local scope pointer is explicitly **not modified** by this change (design §6) — it is read by Slice C, never written to or restructured by any task here.

# Archive report: pi-program-status-reconciliation

## Outcome

`pi-program-status-reconciliation` is archived as a **superseded proposal-only
historical record**. It is not evidence of a verified or completed
implementation.

## Why it was superseded

The proposal's point-in-time facts no longer match this repository:

- it names `drenyra-ai@0.2.0`; the pinned runtime is now
  `drenyra-ai@0.4.1`;
- it records 16 commands; the registered surface is now 20 commands;
- it describes `package-contract` and `runtime-dependency` as `0.1-draft`;
  the Pi-local contract surface is Frozen at v0.1.

The current point-in-time source of truth is
[`docs/architecture/capability-conformance-matrix.md`](../../../../docs/architecture/capability-conformance-matrix.md).

## Preserved lifecycle state

Only the original `proposal.md` is preserved in this archive. The proposal did
not reach design, tasks, apply, or verify, so this archive report does not create
or imply any missing phase artifact, verification result, receipt, or completed
implementation.

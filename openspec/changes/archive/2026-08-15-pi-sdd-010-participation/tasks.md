# Tasks — Pi Participation in SDD-010

> Change: `pi-sdd-010-participation` · Repo: `drenyra-pi` · Status: tasks drafted
> Store: HYBRID — this file is authoritative; Engram is best-effort
> Baseline: checked-out `main` (`c354274`), intentionally dirty, **no commit / no PR / no publish**
> Program authority: `arkelythex/drenyra-ai@4975f4f` (read-only; never write master)
> Delivery: uncommitted by design — see Review Workload Forecast (delivery is not committed in this change; no commit, PR, or publish is created)
> Strict TDD: `bun test` (vitest). RED → GREEN → TRIANGULATE → REFACTOR for code-adding tasks.
> Scope bound: §13 apply whitelist only. `.codegraph/` and all master-repo paths always excluded. No blanket reset/clean/stage.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ≈705–1,100 (planning estimate; apply must measure actual additions+deletions; excludes mechanical `contracts/SHA256SUMS.json` regeneration churn) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | WU1 → WU2 → WU3 → WU4 → WU5 (five reviewable work units) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

**Why High / Why Yes:** this change authors two new scripts (~120–180 and ~100–140 lines), two new test files (~80–140 each), a capability manifest (~90–130 lines), a lock-facts JSON (~60–90), the apply-progress claim matrix, plus contract/config/ROADMAP edits — a planning total materially above one reviewable 400-line diff. Delivery is **uncommitted by design** (no commit/PR/publish in this change, per REQ-BOUND-003), so "chained PRs recommended" here means the apply work must be split into the five bounded, independently-verifiable work units (WU1..WU5) rather than one monolithic application; there are no actual merged PRs. Per the design §14 note and the SDD Review Workload Guard, the `ask-on-risk` guard applies even though delivery is uncommitted — the orchestrator must confirm the WU boundary (and the missing `chain_strategy`) with the user before apply starts.

### Per-work-unit estimate (planning budgets)

| WU | Boundary | Est. files | Est. lines | Size watch |
|----|----------|-----------|------------|------------|
| WU1 | Spec move + RED baseline + release assertion | 4 | 15–35 | — |
| WU2 | Capability checkpoint (TDD) | 4 | 295–455 | Yes — validator is the largest single new file |
| WU3 | Candidate identity + lock facts (TDD) | 3 | 240–370 | Yes — identity script + lock-facts test |
| WU4 | Claim matrix + contract freeze + manifest regen | 4 | 110–170 | Excludes mechanical SHA256SUMS regen |
| WU5 | Final evidence + planning state + identity finalize | 5 | 45–70 | — |
| **Total** | | | **≈705–1,100** | **>400 → High** |

## Global rules (apply to every task)

- **Whitelist only (§13):** create/modify/move/delete **only** the §13 paths. `proposal.md` and `design.md` are read-only planning inputs (in the candidate-identity path set, never mutated). Any unexpected path is a hard scope failure — do not clean/reset/stage/repair it; report and stop.
- **No commit / no PR / no publish / no master edits / no touching unrelated dirty files** (REQ-BOUND-001..005).
- **Strict TDD:** code-adding tasks write the failing test first (RED), then the minimal implementation (GREEN), then triangulate edge cases (TRIANGULATE).
- **Evidence citation:** every conclusion and claim recorded in `apply-progress.md` must cite its source path and test path with an observed value and the exact command/result. An uncited claim is not evidence.
- **Self-reference normalization (D4):** when computing the candidate identity, normalize only the `candidateIdentity` field in `program-lock-facts.json`, the `current_test_state.candidate_identity` scalar in `openspec/config.yaml`, and values after the literal label `Candidate identity:` in `apply-progress.md`. All other bytes remain identity-bearing.
- **Manifest ordering invariant (D5/§8.3):** finalize both contract bytes and the index first, then regenerate `contracts/SHA256SUMS.json` once, then run focused package verification, then make **no further edit** to any covered file.
- Runtime harness evidence is `N/A` for the new checkpoint files (no new runtime behavior); existing runtime source is exercised through installer/doctor/resolve/pin/package suites.

---

## Work unit 1 — Spec layout and RED baseline

- [x] **T1.1 — Capture and record the RED baseline.** Run the full suite exactly once and record the observed failures in `apply-progress.md` (expected: the release-state assertion and the package content-manifest mismatch = **2 failures**). Make **no** completion claim from this run. Evidence command: `bun test`. Rollback boundary: none (observation only; no mutation). <!-- sdd-owner: implementation -->

- [x] **T1.2 — Move the flat spec into `specs/participation/spec.md` and add the one-domain index.** Delete `openspec/changes/pi-sdd-010-participation/spec.md` only after `openspec/changes/pi-sdd-010-participation/specs/participation/spec.md` carries byte-equivalent requirement semantics (REQ-BASE/CON/CONF/CAP/LOCK/ROAD/BOUND), and add `openspec/changes/pi-sdd-010-participation/specs/README.md` indexing the single `participation` domain with the seven requirement families. Do **not** create `openspec/specs/participation/spec.md` (canonical spec layout is out of scope, D1). Evidence: `git status --short openspec/changes/pi-sdd-010-participation/` shows the move; `git diff --no-index --stat <old> <new>` negligible semantic delta; `bun test` still shows only the same 2 baseline failures. Rollback boundary: reverse the move without changing requirement content (restore `spec.md`, remove `specs/`). <!-- sdd-owner: implementation -->

- [x] **T1.3 — Correct the release-state assertion to reconciled public state.** In `__tests__/release-verify-workflow.test.ts` rename the describe/test labels at lines 203–204 from private to public repository state and replace only the obsolete `expect(releasing).toContain("private")` (line 207) with the public-state block. The resulting first test must be exactly: `expect(releasing).toContain("release-verify.yml");`, `expect(releasing).toMatch(/no publish|verification-only|does not publish/i);`, `expect(releasing).toContain("public");`. Leave the existing second test unchanged: `expect(releasing).toMatch(/future publish/);` and `expect(releasing).not.toContain("npm publish");`. Do not weaken, remove, or rename the verification-only safeguards. Evidence: `bun test __tests__/release-verify-workflow.test.ts` → that test passes while still asserting `release-verify.yml`, the no-publish match, `/future publish/`, and `not.toContain("npm publish")`. Rollback boundary: restore only the private-state label and assertion changed here. <!-- sdd-owner: implementation -->

---

## Work unit 2 — Capability checkpoint validator (strict TDD)

- [x] **T2.1 — RED: write deterministic capability-manifest CLI tests.** Add `__tests__/capability-manifest.test.ts` that spawns `scripts/verify-capability-manifest.mjs` against deterministic temporary roots and covers: a valid manifest passes; unknown capability fails; missing role fails; invalid YAML/JSON fails; implemented state without source/test evidence fails; inconsistent counts (`passed + failed !== total`) fail. A final real-repository case validates the root `capability-manifest.yaml`. Run `bun test __tests__/capability-manifest.test.ts` — it must fail until the validator/artifact exist (RED). Rollback boundary: remove the test file alone. <!-- sdd-owner: implementation -->

- [x] **T2.2 — GREEN: capability manifest + validator + wiring.** Create repository-root `capability-manifest.yaml` using the JSON-compatible YAML 1.2 profile (UTF-8 JSON object bytes + final newline; no YAML anchors/tags/implicit scalars), schema `drenyra.capability-manifest.v1`, repository role `agentic-runtime`, version equal to `package.json` version, exactly the ten master Pi capability names (REQ-CAP-001), `testState` with command `bun test`, and states per design §5.2 table (implemented must cite one existing local source + one existing local test; `model-routing` and `engram-integration` are `partial` with non-empty limitation; `configurator-install-doctor-sync` is `planned` with a plan). Add `scripts/verify-capability-manifest.mjs` (zero-dependency ESM CLI, read-only): exit `0` prints `verify-capability-manifest: OK`; exit `1` prints `verify-capability-manifest: FAILED` + one line per violation (unknown/missing capability, missing role, unsupported state, implemented-without-evidence, partial-without-limitation, planned-without-plan, missing evidence path, count arithmetic); exit `2` for invalid CLI usage/unreadable file. Add only `"verify:capability": "node scripts/verify-capability-manifest.mjs"` to `package.json` — do **not** touch `prepack`/`prepublishOnly` and do not add any publish gate. Evidence: `node scripts/verify-capability-manifest.mjs` → exit 0 + `verify-capability-manifest: OK`; `bun run verify:capability` passes; `bun test __tests__/capability-manifest.test.ts` green. Rollback boundary: manifest + validator + test + package script entry together. <!-- sdd-owner: implementation -->

- [x] **T2.3 — TRIANGULATE: evidence and count edge cases.** Extend `__tests__/capability-manifest.test.ts` to also reject: unsupported state value, missing capability (of the ten), `partial` without limitation, `planned` without plan, and an evidence path that does not exist or contains `..`. Confirm the real `capability-manifest.yaml` passes with correct count arithmetic (`passed + failed === total`). Evidence: `bun test __tests__/capability-manifest.test.ts` and `bun run verify:capability` both green. Rollback boundary: same as T2.2 (capability unit). <!-- sdd-owner: implementation -->

---

## Work unit 3 — Candidate identity and lock facts (strict TDD)

- [x] **T3.1 — RED: write lock-facts shape/cross-artifact/re-derivation tests.** Add `__tests__/lock-facts.test.ts` validating `docs/architecture/program-lock-facts.json`: exact schema `drenyra.program-lock-facts.v1`, `participantCheckpoint: true`, authority notice present; full 40-char lowercase HEAD SHA (starts `c354274`) and a distinct `dirty-sha256:<64 lowercase hex>` candidate identity; `packageVersion === package.json.version`; consumed set contains `mission-protocol@0.1`, `candidate@0.1`, `receipt@0.1`, `gate@0.1`, `ledger@0.1`, `recovery@0.1` and produced set contains `package-contract@0.1`, `runtime-dependency@0.1` (names/versions unique); integer test arithmetic with zero failures; pin checksum equals `DEFAULT_PIN.checksumSha256` in `runtime/pin.ts`; manifest + capability digests match current bytes; capability reference/state counts agree with `capability-manifest.yaml`; `activeChanges` sorted and includes `pi-sdd-010-participation`; and candidate identity re-derived by `node scripts/compute-candidate-identity.mjs` equals the recorded value. Run `bun test __tests__/lock-facts.test.ts` — it must fail until the identity script and lock facts exist (RED). Rollback boundary: remove the test file alone. <!-- sdd-owner: implementation -->

- [x] **T3.2 — GREEN: candidate identity CLI.** Add `scripts/compute-candidate-identity.mjs` implementing the design §7.2 canonical algorithm **exactly**: (1) `git rev-parse HEAD` → full 40 lowercase hex; (2) immutable lexicographically-sorted `PARTICIPATION_PATHS_V1` = the §13 apply whitelist plus `openspec/changes/pi-sdd-010-participation/proposal.md` and `design.md`; (3) classify changed entries `A`/`M`/`D` (staged + unstaged; existing file hashes working-tree bytes; deleted digest is `-`); (4) Git mode `100644`/`100755` from HEAD for tracked, executable bit for new, HEAD mode for deletions; (5) normalize self-references in `program-lock-facts.json` (`candidateIdentity` → `__CANDIDATE_IDENTITY__`, compact JSON, recursively lexicographically-sorted keys, final newline), `openspec/config.yaml` (`current_test_state.candidate_identity` scalar only), and `apply-progress.md` (values after literal label `Candidate identity:` only); (6) lowercase sha-256 per normalized byte sequence; (7) build the canonical manifest with NUL separators in the exact `candidate-format\0…\n head\0…\n path\0…` shape, entries sorted by path; (8) sha-256 the manifest, prefix `dirty-sha256:`. Prints one line `dirty-sha256:<64 lowercase hex>` and exits non-zero if Git/HEAD unreadable, an allowlisted path cannot be classified, normalization fails, or no allowlisted candidate change exists. Evidence: `node scripts/compute-candidate-identity.mjs` → valid one-line identity, exit 0. Rollback boundary: remove the script alone. <!-- sdd-owner: implementation -->

- [x] **T3.3 — GREEN: static lock-facts artifact.** Create `docs/architecture/program-lock-facts.json` per design §6.2 exact schema: `participantCheckpoint: true`, `authorityNotice: "Pi-local input; does not modify or promote the program master"`, `headSha` from `git rev-parse HEAD`, `candidateIdentity` initially a placeholder (finalized in WU5 per §7.3), `packageVersion`, `contracts.consumed`/`produced`, `tests` (command `bun test`, counts from observed output), `checksums` (pin entry sha-256, content-manifest path + digest), `capabilityStates` (manifest path/schema/digest), `activeChanges` (sorted, includes this change), `evidenceDate`, and `derivationCommands` (HEAD query, identity command, full test, typecheck, package verification, style, capability, manifest-file checksum). Author from actual observed output, not a generator. Evidence: `bun test __tests__/lock-facts.test.ts` green. Rollback boundary: lock-facts artifact + identity script + lock-facts test together. <!-- sdd-owner: implementation -->

- [x] **T3.4 — TRIANGULATE: identity edge cases.** Extend `__tests__/lock-facts.test.ts` to verify: modified/new/deleted path entries all classified correctly (the flat-spec delete, new files, modified contracts), sorted canonical-manifest ordering, self-reference normalization produces a stable identity, an invalid/missing digest is rejected, and a mismatched pin checksum, package version, capability digest, or active-change set fails validation. Confirm re-derivation via the CLI equals the recorded value. Evidence: `bun test __tests__/lock-facts.test.ts` green and `node scripts/compute-candidate-identity.mjs` stable across two runs. Rollback boundary: same as T3.3 (identity/lock-facts unit). <!-- sdd-owner: implementation -->

---

## Work unit 4 — Contract claim check and freeze

- [x] **T4.1 — Complete the claim matrix in `apply-progress.md`.** Record per design §8.2 the five claim rows — 16 commands (`extensions/register.ts` vs `__tests__/extension.test.ts`), seven agents (`agents/*.md` vs `__tests__/agents.test.ts` + `__tests__/extension.test.ts` package mirrors), pin/version/state/checksum (`runtime/pin.ts` vs `__tests__/pin.test.ts` + `__tests__/package-verify.test.ts`), package-local/no-PATH (`runtime/resolve.ts` vs `__tests__/resolve.test.ts`), checksum + fail-closed doctor (`runtime/checksum.ts` + `runtime/doctor.ts` vs `__tests__/doctor.test.ts` + `__tests__/status.test.ts`). Also record the REQ-CONF-001 conformance mapping (install/doctor/pin/package-integrity/release-verification → named existing tests) and the no-gap decision **not** to add `__tests__/contracts-conformance.test.ts`. Each row records `match` or `blocked` with the observed value, exact source path, exact test path, and command/result. Any `blocked` row prevents the corresponding freeze. Evidence: readback of the completed matrix in `apply-progress.md`; each row's focused test command (e.g. `bun test __tests__/extension.test.ts`, `__tests__/agents.test.ts`, `__tests__/pin.test.ts`, `__tests__/resolve.test.ts`, `__tests__/doctor.test.ts`, `__tests__/status.test.ts`). Rollback boundary: remove/restore only the matrix section in `apply-progress.md`. <!-- sdd-owner: implementation -->

- [x] **T4.2 — Freeze both contract headers and the index (only if every applicable claim row is `match`).** In `contracts/package-contract.md` change `> Version: 0.1-draft · Status: draft ·` to `> Version: v0.1 · Status: frozen ·` (retain the exact 16-command list, seven-agent inventory, and released `drenyra-ai@0.2.0` checksum claim; correct prose only on a source-check mismatch). In `contracts/runtime-dependency.md` change `> Version: 0.1-draft · Status: draft ·` to `> Version: v0.1 · Status: frozen ·` (pin, package-locality, no-PATH, checksum, doctor, fail-closed claims frozen only if they match runtime source/tests). In `contracts/README.md`: replace the draft pre-alpha status sentence with a frozen-v0.1 statement for these two local contracts; change both index versions `0.1-draft` → `0.1` and both statuses `Draft` → `Frozen`; leave unrelated rows unchanged. Do **not** freeze if any applicable §8.2 claim row is `blocked` (then stop and return to design/tasks). Evidence: `git diff -- contracts/package-contract.md contracts/runtime-dependency.md contracts/README.md` shows the header/index changes and retained claims; `grep -n "frozen" contracts/*.md` confirms both headers and both README rows. Rollback boundary: the three contract docs as one consistency unit (restore draft headers + README draft rows together). <!-- sdd-owner: implementation -->

- [x] **T4.3 — Regenerate the package content manifest once, then verify.** After the final covered bytes of both contracts and the index are stable, run `node scripts/verify-package-files.mjs --update` to regenerate `contracts/SHA256SUMS.json` (this is the only allowed writer for that manifest). Immediately run `bun test __tests__/package-verify.test.ts` and `node scripts/verify-package-files.mjs`. Make **no further covered-file edits** (any later covered-file edit invalidates the evidence and requires regeneration plus both commands again). Evidence: `node scripts/verify-package-files.mjs --update` succeeds; `bun test __tests__/package-verify.test.ts` green (incl. the reconciliation assertion at line 181); `node scripts/verify-package-files.mjs` exit 0. Rollback boundary: restore both contract docs + index to draft and restore/regenerate `SHA256SUMS.json` consistently, as one unit. <!-- sdd-owner: implementation -->

---

## Work unit 5 — Final evidence and planning state

- [x] **T5.1 — Capture full-suite final evidence.** Run the complete final-candidate verification set and record observed counts/exit codes in `apply-progress.md` and later into the manifest/lock facts: `bun test` (final files/passed/failed/total; must be zero failures before any completion claim), `bun run typecheck`, `node scripts/verify-package-files.mjs`, `bun run verify:style`, `bun run verify:capability`. Evidence: exact command output and exit codes recorded; `bun test` zero failures. Rollback boundary: none (observation only). <!-- sdd-owner: implementation -->

- [x] **T5.2 — Populate manifest `testState` and lock facts from observed output + active-changes discovery.** Set `capability-manifest.yaml` `testState` (command `bun test`, `result: passing`, files/passed/failed/total from T5.1, `evidenceRef` to lock facts `#/tests`) and finalize `docs/architecture/program-lock-facts.json` `tests`, `checksums`, `capabilityStates.digestSha256`, `contentManifest.sha256`, `evidenceDate`, `derivationCommands`, and `activeChanges` (sorted, including `pi-sdd-010-participation` and every real local OpenSpec change discovered at evidence time — do not copy the stale master row). Enforce §12 cross-artifact invariants: `package.json.version === manifest version === lock packageVersion`; capability test counts `===` lock tests `===` config `current_test_state`; manifest digest in lock facts `===` current manifest bytes; pin checksum `=== runtime/pin.ts`; content-manifest digest `=== contracts/SHA256SUMS.json` bytes. Evidence: `bun test __tests__/lock-facts.test.ts` green; `bun run verify:capability` green; digest readback `sha256sum capability-manifest.yaml contracts/SHA256SUMS.json`. Rollback boundary: the two participant artifacts together. <!-- sdd-owner: implementation -->

- [x] **T5.3 — Update only the four ROADMAP Phase 1 items (evidence-gated).** In `ROADMAP.md`, check **only** these four Phase 1 lines, each only after its evidence condition is true: `Freeze package-contract v0.1 (install surface, provided capabilities, versioning)` (header frozen, README row Frozen, command/agent/pin claim rows all `match`, package verification green); `Freeze runtime-dependency v0.1 (pin strategy, verification, package-locality)` (header frozen, README row Frozen, pin/resolve/doctor claim rows all `match`, focused suites green); `Command contract: /drenyra:* surface and expected outputs` (exact 16-command `__tests__/extension.test.ts` assertions green and mapped in apply evidence); `Conformance tests for install/doctor/pin verification` (installer/doctor/resolve/pin/package map complete and full suite green; no duplicate-test gap). Do **not** change Phase 0, Phase 2, Phase 3, national-alignment, Gate 0, or SDD-020 lines. Evidence: `grep -n "\[x\]" ROADMAP.md` shows exactly these four new checks; readback that no other section changed. Rollback boundary: restore only these four ROADMAP lines to unchecked. <!-- sdd-owner: implementation -->

- [x] **T5.4 — Replace `openspec/config.yaml` `current_test_state` with final evidence.** Remove the archived 493-test evidence claim; write `current_test_state` with observed values only: `files`, `tests` (final total), `passing` (true only when failed=0), `failed` (final), `command: bun test`, `candidate_identity: dirty-sha256:<64 lowercase hex>`, `evidence_date: 2026-08-14`, and the `evidence` block pointing to `docs/architecture/program-lock-facts.json` and the change verify report. Values must equal `program-lock-facts.json.tests`; the candidate identity is normalized only for D4 hashing. Evidence: readback of `openspec/config.yaml`; `bun test __tests__/lock-facts.test.ts` cross-check. Rollback boundary: restore only the `current_test_state` block. <!-- sdd-owner: implementation -->

- [x] **T5.5 — Finalize the candidate identity (write → compute → write → recompute).** Per design §7.3: (1) ensure all implementation/evidence/planning bytes hold `candidateIdentity` placeholders; (2) run `node scripts/compute-candidate-identity.mjs`; (3) write the same value into `program-lock-facts.json.candidateIdentity`, `openspec/config.yaml.current_test_state.candidate_identity`, and each `Candidate identity:` label in `apply-progress.md`; (4) recompute — it must be identical (only normalized fields changed). Evidence: the two CLI runs print identical `dirty-sha256:<64hex>`; `bun test __tests__/lock-facts.test.ts` re-derivation assertion green. Rollback boundary: restore placeholders and revert only the identity-bearing fields. <!-- sdd-owner: implementation -->

- [x] **T5.6 — Final exact-candidate verification pass (no source mutation).** Rerun `bun test`, `bun run typecheck`, `node scripts/verify-package-files.mjs`, `bun run verify:style`, and `bun run verify:capability` against the exact same bytes. If counts or bytes differ, update the evidence (manifest/lock facts/config), recompute the identity, and repeat once from the affected invariant; never claim stale evidence. Confirm the changed-path set equals the §13 whitelist and that no pre-existing out-of-scope dirty path was modified, staged, or removed. Evidence: all five checks green on the identical bytes; `git status --short` and changed-path comparison against the whitelist show no out-of-scope mutation; final identity recorded. Rollback boundary: none (verification only); on failure, fix evidence per this task's repeat-once rule. <!-- sdd-owner: implementation -->

---

## Parent-owned lifecycle gates (post-apply, per work unit)

These run after the implementation tasks of each work unit are normalized, reviewed, and verified. They are owned by the orchestrator/parent, not by apply.

- [ ] **T-GATE-001 — Confirm the WU boundary and missing chain strategy before apply.** Present the five work-unit boundary plan and the `ask-on-risk` workload guard (est. ≈705–1,100 lines, >400) plus the missing `chain_strategy` (stacked-to-main vs feature-branch-chain) to the user; do not start apply until confirmed. <!-- sdd-owner: parent -->
- [ ] **T-GATE-002 — Per-work-unit bounded review and delivery.** For each WU1–WU5: run source-mutating normalization first, start or reuse the bounded review on the frozen candidate, then validate the pre-commit/pre-push/pre-PR gates with the approved receipt and deliver exactly the reviewed bytes (work-unit-commits skill: behavior + tests + docs in the same unit). This change creates **no commit, PR, or publish** — delivery stays uncommitted per REQ-BOUND-003. <!-- sdd-owner: parent -->
- [ ] **T-GATE-003 — Boundary and master-compliance verification.** After WU5, confirm in the verify phase: no gated SDD (SDD-020/030/040) implemented, no master-owned file changed under `arkelythex/drenyra-ai`, no publish/commit/PR created, no unrelated dirty file modified, and Gate 0 never claimed complete (REQ-BOUND-001..005). <!-- sdd-owner: parent -->
- [ ] **T-GATE-004 — Final verify and archive.** Run `sdd-verify` against the moved spec (`specs/participation/spec.md`), confirm `activeChanges` includes this change while active, and escalate any CRITICAL finding before `sdd-archive`. <!-- sdd-owner: parent -->

## Definition of done and rollback

- RED baseline captured (2 known failures) with no completion claim; release assertion corrected to reconciled public state.
- Capability manifest validates under `verify:capability` with exactly ten master names and correct state evidence.
- Lock facts validate under `__tests__/lock-facts.test.ts`; candidate identity is deterministic and re-derived identically.
- Both contracts frozen `v0.1`; README rows `0.1 / Frozen`; `SHA256SUMS.json` regenerated after final bytes and package verification green.
- Claim matrix complete with `match`/`blocked` per row; no dedicated conformance test added (gap closed by existing assertions).
- ROADMAP four Phase 1 items checked only on fresh final-candidate evidence; `current_test_state` refreshed from observed output; archived 493-test claim removed.
- Full suite green on the exact final candidate; changed-path set equals the §13 whitelist; no commit/PR/publish/master edit.
- Rollback is path-scoped per WU (see each task's rollback boundary). Never use blanket reset/clean and never touch master or unrelated dirty paths.

# OpenSpec Specs — pi-sdd-030-routing-adapter

This directory indexes the change-local requirement domain for
`pi-sdd-030-routing-adapter` (Add Deterministic Work Routing to Drenyra Pi).

This is change-local organization only, following the
`pi-sdd-040-adapter-boundary` precedent (`pi-sdd-010-participation` also uses a
single change-local domain per change). The routing adapter is a runtime domain:
at archive time, per the OpenSpec convention, the full domain spec is copied to
the canonical `openspec/specs/routing-adapter/spec.md`.

## Domains

| Domain | Spec | Requirement families |
| --- | --- | --- |
| routing-adapter | [routing-adapter/spec.md](routing-adapter/spec.md) | REQ-PRE, REQ-ROUTE, REQ-EXEC, REQ-INTEG, REQ-BOUND |

# /drenyra:period

Set the fiscal period context (YYYYMM with a real month) for the session.

## Output

- Confirmation of the persisted period, or the validation error. The period
  feeds the canonical scope for every protected command.

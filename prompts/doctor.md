# /drenyra:doctor

Run the fail-closed runtime doctor against the pinned Drenyra AI runtime.

## Output

- The doctor verdict and the full report: pin package, expected version,
  installed state, and any discrepancy.
- The command never grants mission capability; it only reports runtime
  health.
